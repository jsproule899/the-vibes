{
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"],
      "proxy": {
        "backendUri": "https://api.thevibes.fun/api/{*}"
      }
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/api/*",
      "/images/*.png",
      "/images/*.jpg",
      "/images/*.gif",
      "/css/*",
      "/assets/*",
      "/img/*"
    ]
  }
}

{
  "routes": [
    {
      "route": "/api/{*rest}",
      "allowedRoles": ["anonymous"],
      "proxy": {
        "backendUri": "https://api.thevibes.fun/api/{rest}"
      }
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/api/*",
      "/images/*.png",
      "/images/*.jpg",
      "/images/*.gif",
      "/css/*",
      "/assets/*",
      "/img/*"
    ]
  }
}
